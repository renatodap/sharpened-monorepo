# --- Supabase Project ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service role NEVER goes to client/build; only used in server/seed, CI jobs
SUPABASE_SERVICE_ROLE_KEY=

# --- App Defaults ---
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_DEFAULT_UNITS_WEIGHT=kg   # kg | lb
NEXT_PUBLIC_DEFAULT_UNITS_DISTANCE=km # km | mi

# --- OpenAI (coach, later step) ---
OPENAI_API_KEY=
